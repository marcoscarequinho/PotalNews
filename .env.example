# Database Configuration
# For production, use your Neon/PostgreSQL database URL
DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require
POSTGRES_URL=postgresql://username:password@host:port/database?sslmode=require

# Authentication Mode
# Options: 'mock' (development), 'passport' (production), 'disabled'
AUTH_MODE=mock

# Session Secret
# Use a strong, random secret in production
SESSION_SECRET=your-very-secure-session-secret-here

# Port Configuration (for local development)
PORT=5000

# Node Environment
NODE_ENV=development

# Vercel Environment
VERCEL_ENV=development

# Google Cloud Storage (if using file uploads)
GOOGLE_CLOUD_PROJECT_ID=your-project-id
GOOGLE_CLOUD_STORAGE_BUCKET=your-bucket-name

# Additional Vercel-specific variables that might be needed
# VERCEL_URL (automatically set by Vercel)
# VERCEL_BRANCH_URL (automatically set by Vercel)
# VERCEL_REGION (automatically set by Vercel)